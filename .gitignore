# compiled binaries
/api
/tracker-api
/repoll
/syncer
*.exe
# local env / secrets
.env
.env.local
# go test/coverage artifacts
*.out
coverage.*
# estado local do code-graph MCP (tokensave)
.tokensave/
